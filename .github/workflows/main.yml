
on: [push]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - uses: peaceiris/actions-gh-pages@main
      - run: echo 'run tests'
      - with:
          publish_dir: ./docs
