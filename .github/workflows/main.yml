on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    name: Test   
    steps:
      - uses: actions/checkout@main
      - uses: peaceiris/actions-gh-pages@main
      with:
          publish_dir: ./docs